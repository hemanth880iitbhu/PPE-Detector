streamlit==1.36.0

# PyTorch Ecosystem: Must be compatible with each other and the Python environment (e.g., Python 3.10+)
# torch 2.5.0 requires newer torchvision/torchaudio.
torch==2.9.0
torchvision==0.24.0   # Updated from 0.16.0 to a version compatible with torch 2.5.0
torchaudio==2.9.0   # Updated from 2.1.0 to match the torch 2.5.0 version

# Core dependencies (ensuring compatibility with Python environment)
ultralytics>=8.1.3
opencv-python-headless>=4.7.0
Pillow>=10.0.0           # Good constraint, keeping it below major version 10
pandas>=1.5.0
huggingface-hub>=0.20.0
altair>=4.2.0

# Telegram
python-telegram-bot
